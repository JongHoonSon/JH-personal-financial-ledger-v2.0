mixin list(category)
    div.user-own-categories__category__wrap(data-category=`${category}`)
        li.user-own-categories__category=category
        button.user-own-categories__category__del-button
            i.fa-solid.fa-circle-minus.user-own-categories__category__del-button__icon(data-category=`${category}`)